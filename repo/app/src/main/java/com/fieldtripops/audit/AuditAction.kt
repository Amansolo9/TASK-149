package com.fieldtripops.audit

enum class AuditAction {
    LOGIN_SUCCESS,
    LOGIN_FAILED,
    LOGOUT,
    SESSION_CREATED,
    SESSION_EXPIRED,
    SESSION_TOUCH,
    CONSENT_GRANTED,
    CONSENT_REVOKED,
    USER_CREATED,
    USER_UPDATED,
    ROLE_ASSIGNED,
    ROLE_REVOKED,
    ATTACHMENT_CREATED,
    ATTACHMENT_DELETED,
    FIELD_DECRYPTED,
    ACCOUNT_LOCKED,
    ACCOUNT_UNLOCKED,

    // Booking
    ITINERARY_SAVED,
    ITINERARY_DELETED,
    BOOKING_SUBMITTED,
    BOOKING_CONFIRMED,
    BOOKING_CANCELLED,
    BOOKING_AUTO_CLOSED,
    BOOKING_STATE_CHANGED,
    QUOTA_RESERVED,
    QUOTA_CONFIRMED,
    QUOTA_RELEASED,
    QUOTA_EXCEPTION,
    FEE_ITEMS_UPDATED,

    // Phase 3 — Reschedules, Refunds, Claims
    RESCHEDULE_REQUESTED,
    RESCHEDULE_EXCEPTION,
    RESCHEDULE_APPROVED,
    RESCHEDULE_REJECTED,
    REFUND_APPROVED,
    REFUND_OVERRIDE,
    CLAIM_FILED,
    CLAIM_ATTACHMENT_ADDED,
    TICKET_STATE_CHANGED,
    TICKET_AUTO_CLOSED,
    TICKET_APPEAL_FILED,
    INVESTIGATION_NOTE_ADDED,

    // Phase 4 — Governance, Dedup, Rollback
    CONTENT_RATED,
    CONTENT_COMMENTED,
    CONTENT_FAVORITED,
    CONTENT_DOWNLOADED,
    CONTENT_DEMOTED,
    CONTENT_QUARANTINED,
    CONTENT_EXCLUDED,
    CONTENT_RESTORED,
    GOVERNANCE_OVERRIDE,
    DUPLICATE_DETECTED,
    CHECKPOINT_CREATED,
    ROLLBACK_EXECUTED,

    // Phase 5 — Reports, Exports, Retention
    REPORT_GENERATED,
    EXPORT_CREATED,
    EXPORT_DOWNLOADED,
    DATA_DELETED,
    DATA_ANONYMIZED,
    RETENTION_SWEEP_RUN,
    SLA_CHANGED,
    RETENTION_POLICY_CHANGED,

    // Phase 7 — per-user deletion workflow, SLA reminders,
    // claim compensation, refund rule management
    DELETION_REQUESTED,
    DELETION_APPROVED,
    DELETION_REJECTED,
    DELETION_EXECUTED,
    DELETION_FAILED,
    SLA_REMINDER_GENERATED,
    SLA_REMINDER_ACKNOWLEDGED,
    CLAIM_COMPENSATION_SET,
    REFUND_RULE_UPDATED,
    REFUND_RULE_CREATED
}
